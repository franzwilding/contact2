<%= form_for(@city) do |f| %>
  
  <%= render :partial => 'shared/form_error', :locals => {:errors => @city.errors} %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
